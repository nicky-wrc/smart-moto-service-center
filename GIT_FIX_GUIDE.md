# 🔧 คู่มือจัดการ Git Status

## 📊 สถานการณ์

หลังจาก pull กลับมาจาก GitHub พบว่า:
- **53 ไฟล์ Modified** - ส่วนใหญ่เป็นเรื่อง **line endings** (CRLF ↔ LF)
- **4 ไฟล์ใหม่** - ไฟล์ที่เพิ่งสร้าง (CI/CD, Documentation)

## ✅ สิ่งที่ทำไปแล้ว

1. ✅ สร้าง `.gitattributes` - เพื่อจัดการ line endings ในอนาคต
2. ✅ Add ไฟล์ใหม่เข้า staging area

## 🎯 ทางเลือกในการจัดการ

### ตัวเลือก 1: Normalize Line Endings แล้ว Commit (แนะนำ)

ถ้าต้องการให้ไฟล์ใน repository ใช้ LF consistently:

```bash
# 1. Add .gitattributes (ทำแล้ว)
git add .gitattributes

# 2. Normalize line endings ของไฟล์ทั้งหมด
git add --renormalize .

# 3. ตรวจสอบว่าเหลืออะไร
git status

# 4. Commit
git commit -m "chore: normalize line endings and add .gitattributes"
```

### ตัวเลือก 2: Discard Line Ending Changes (ถ้าไม่ต้องการ commit)

ถ้าไม่ต้องการ commit การเปลี่ยนแปลง line endings:

```bash
# Restore ไฟล์ที่ modified (จะเก็บเฉพาะไฟล์ใหม่)
git restore backend/src/

# หรือ restore ทุกอย่างยกเว้นไฟล์ใหม่
git restore .
```

### ตัวเลือก 3: Commit แยกกัน

```bash
# 1. Commit ไฟล์ใหม่ก่อน
git add .github/ backend/BACKEND_LEAD_STATUS.md backend/GIT_CONVENTIONS.md
git commit -m "docs: add CI/CD workflow, Git conventions, and status documentation"

# 2. Normalize และ commit line endings
git add --renormalize .
git commit -m "chore: normalize line endings with .gitattributes"
```

## 💡 คำแนะนำ

**แนะนำใช้ตัวเลือก 1** เพราะ:
- ✅ ป้องกันปัญหา line endings ในอนาคต
- ✅ ทำให้ repository สะอาดและ consistent
- ✅ Team members จะไม่เจอปัญหาเดียวกัน

## ⚠️ หมายเหตุ

- การ normalize line endings จะเปลี่ยน CRLF → LF ในไฟล์ `.ts`, `.js`, `.json`, `.md` เป็นต้น
- ไฟล์ใหม่ที่สร้างจะใช้ LF ตาม `.gitattributes`
- Windows จะยังสามารถ edit ได้ปกติ (Git จะแปลงอัตโนมัติ)

## 📝 ขั้นตอนต่อไป

หลังจาก commit แล้ว:
```bash
# Push ขึ้น GitHub
git push origin Nicky_dev
```


