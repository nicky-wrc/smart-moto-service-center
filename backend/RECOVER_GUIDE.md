# 🆘 คู่มือกู้คืนไฟล์ที่หายไป

## สถานการณ์
ไฟล์ที่ Cursor สร้างหายไปหลังจากปิด Cursor โดยยังไม่ได้ save

## 🔍 วิธีตรวจสอบ

### 1. ตรวจสอบ Git Status
```bash
git status
```

### 2. ตรวจสอบไฟล์ที่แก้ไขล่าสุด (24 ชั่วโมงที่แล้ว)
```bash
# PowerShell
Get-ChildItem -Path "backend/src" -Recurse -File | Where-Object { $_.LastWriteTime -gt (Get-Date).AddHours(-24) } | Select-Object FullName, LastWriteTime
```

## 🔄 วิธีกู้คืน

### วิธีที่ 1: ใช้ Cursor Conversation History (แนะนำ)

1. **เปิด Cursor**
2. **ดู Conversation History** (ถ้ายังเปิดอยู่)
3. **หา code ที่ Cursor generate ให้**
4. **Copy code จาก conversation และ paste กลับเข้าไฟล์**

### วิธีที่ 2: ใช้ VS Code Local History (ถ้าเปิดไว้)

1. **เปิด VS Code**
2. **Right-click ที่ไฟล์ที่หาย**
3. **เลือก "Local History" → "Timeline"**
4. **ดู history และ restore version ที่ต้องการ**

### วิธีที่ 3: ตรวจสอบ Backup Files

บางครั้ง Cursor หรือ VS Code อาจจะสร้าง backup files:
- `.vscode/` folder
- `*.bak` files
- Temporary files

### วิธีที่ 4: ใช้ Git (ถ้าไฟล์ถูก track ไว้)

```bash
# ดู diff ของไฟล์ที่แก้ไข
git diff <file-path>

# Restore ไฟล์ที่ถูกแก้ไข
git restore <file-path>

# ดู untracked files
git status --untracked-files=all
```

## ⚠️ คำแนะนำสำหรับอนาคต

### 1. Save บ่อยๆ
- กด `Ctrl+S` บ่อยๆ
- ใช้ Auto Save ใน VS Code

### 2. Commit บ่อยๆ
```bash
# Commit เมื่อ feature เล็กๆ เสร็จ
git add .
git commit -m "feat: add feature name"
```

### 3. ใช้ Git Stash (ถ้ายังไม่พร้อม commit)
```bash
# เก็บ changes ชั่วคราว
git stash

# นำกลับมา
git stash pop
```

### 4. เปิด Auto Save ใน VS Code
- File → Auto Save (หรือ `Ctrl+K S`)

## 🆘 ถ้ายังกู้คืนไม่ได้

**ขั้นตอนสุดท้าย:**
1. ดู Cursor conversation history
2. Copy code จาก conversation
3. สร้างไฟล์ใหม่และ paste code เข้าไป

---

**หมายเหตุ:** ไฟล์ที่ยังไม่ได้ save จะไม่ถูกเก็บไว้ใน Git หรือ file system ต้องกู้คืนจาก Cursor conversation history เท่านั้น


